# V151B post-hoc claim boundary

This note changes no V151B result and introduces no new scientific evidence. It records an apparatus fact discovered after V151B completion while reducing its residual under the Rigorous Breakthrough Stack.

The recovered provider used by V151B calls River with `temperature=0.0` while accepting and recording different seed values. Consistent with that implementation, every first-call response across cold, compiled-O1, raw-T1, and both sham arms proposed the same wrong `strip_jsonp` semantic edit. Raw T1 differed only in successfully transporting second-call replicas to the verifier; all six raw-T1 native verifier trials failed.

Therefore the immutable controller verdict `NEGATIVE_V151_NO_T1_DEVELOPMENTAL_SIGNAL_WITHIN_BUDGET` must be cited with this narrower interpretation:

> no T1 retained-state advantage was observed on the single greedy proposal trajectory induced by the V151B provider within two calls.

V151B does **not** establish that three independent sampled discovery opportunities failed, because its three seed labels did not create a nonzero-temperature proposal distribution.

This observation motivated the prospectively frozen V152 exploration-support separator. It does not retroactively change V151B outcomes.