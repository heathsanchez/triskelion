# V146c natural O1 → O2 causal separator — raw-policy representation

Controller: Rigorous Breakthrough Stack v1.1.

V146/V146b were R10 before any O2/youtube arm: the source-verified httpie repair replay succeeded, but the model-produced policy response did not conform to the harness's imposed four-field JSON schema. Those runs provide no O1→O2 semantic evidence.

This preregistration changes only the retained-capability representation and therefore is a new scientific assumption rather than a silent apparatus edit.

## Frozen O1 representation
- Source episode remains `httpie/5`, independently qualified before this experiment.
- Replay the developer intervention and require native verifier PASS before synthesis.
- Ask the same model to compress that one verified repair into a portable repair policy, forbidding project/bug/test identity and case-specific patch copying.
- O1 is the **exact non-empty model response text** after trimming outer whitespace. No schema normalization, field repair, semantic rewriting, or human editing is permitted.
- If the response contains `httpie`, `httpie/5`, the source test path, or another frozen source identifier, O1 is rejected and V146c is not allowed to run O2 arms.
- Freeze response SHA256 and byte length before O2 evaluation.

## O2 target and matched arms
Target remains the pre-existing independently qualified `youtube-dl/32`. Reserved O3 `pandas/66` remains untouched.

Arms, seeds, model, verifier and one-call budget are unchanged from V146:
- D: cold.
- D+O1: exact frozen O1 response text.
- D+sham: deterministic token-order reversal preserving the exact whitespace slots and exact byte length of O1.
- seeds 20260815..20260818, one call per seed/arm, no verifier-feedback retry, Qwen/Qwen3.5-9B, max_tokens=2048, exact historical runtime.

## Strong causal PASS
Requires O1 source replay verified, youtube baseline reproducibly failing, D+O1 ≥1 verified repair, D=0 verified repairs, sham=0 verified repairs, no test edits, no affected R10 arms.

If cold succeeds, reachability is NULL and only preregistered efficiency may be reported. If O1 also fails, O1→O2 is NEGATIVE under the frozen budget. Infrastructure/provider/runtime failure is R10.

Only a strong PASS licenses a new preregistration to synthesize O2 from the verified O1-assisted youtube episode. V146c cannot establish O3 or multi-generation development.