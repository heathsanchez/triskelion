# V152 — Exploration-support separator

Frozen: 2026-08-16 NZST
Controller: Rigorous Breakthrough Stack v1.1

## Residual

V151B produced a valid bounded negative on `youtube-dl/32`, but audit of the recovered CP1 River provider showed `temperature=0.0` while the experiment treated three seed values as three discovery opportunities. All substantive arms and all seeds converged on the same wrong `strip_jsonp` edit. Therefore V151B is evidence about one greedy proposal trajectory, not yet a sampled reachability frontier.

## Question

When the exact V151B experiment is given genuine but fixed stochastic proposal support, does compiled O1 or raw verified T1 history causally improve verified T2 acquisition over matched controls?

## Single intervention

Only model sampling temperature changes:

- V151B: `temperature=0.0`
- V152: `temperature=0.7`

Everything else is inherited unchanged from V151B/V151:

- model `Qwen/Qwen3.5-9B`;
- T1 `httpie/5` and T2 `youtube-dl/32`;
- exact frozen V149 O1 object and SHA-256;
- exact verified T1 intervention and SHA-256;
- arms `D_COLD`, `D_PLUS_O1_COMPILED`, `D_PLUS_RAW_T1`, `D_PLUS_SHAM_O1`, `D_PLUS_SHAM_RAW`;
- seeds `202608161`, `202608162`, `202608163` paired across arms;
- max two calls per seed/arm and max 2048 output tokens;
- V149 context resolver;
- structured exact-replacement edit protocol v2;
- exact historical CPython/native verifier;
- verifier-feedback rule, stopping rule, cost metrics, and arm-advantage classifier.

No temperature search is permitted. `0.7` is frozen before V152 outcomes.

## Diversity eligibility gate

Before any positive causal interpretation, the first model response for each substantive arm (`D_COLD`, `D_PLUS_O1_COMPILED`, `D_PLUS_RAW_T1`) must contain at least **two distinct response SHA-256 hashes across the three frozen seeds**.

If any substantive arm has fewer than two distinct first-response hashes, V152 verdict is:

`R10_INSUFFICIENT_PROPOSAL_DIVERSITY`

regardless of solve counts. This is an apparatus verdict, not a semantic negative.

The diversity gate does not require all three responses to differ and does not inspect semantic quality.

## Scientific classifications

If the diversity gate passes, the unchanged V151 arm classifier applies.

- raw T1 beats cold and matched raw sham while compiled O1 does not: `PASS_V152_CAPABILITY_COMPILATION_LOSS_UNDER_EXPLORATION`;
- compiled O1 beats cold and matched O1 sham: `PASS_V152_COMPILED_O1_CAUSAL_ADVANTAGE_UNDER_EXPLORATION`;
- both compiled and raw beat their matched controls: `PASS_V152_BOTH_RETAINED_STATES_CAUSALLY_USEFUL`;
- neither beats matched controls: `NEGATIVE_V152_NO_T1_DEVELOPMENTAL_SIGNAL_UNDER_SAMPLED_BUDGET`;
- any R10/native apparatus failure remains inconclusive.

A positive is still only a bounded T1→T2 causal result. It does not establish O1→O2→O3 or open-ended development.

## Evidence boundary

V151B remains immutable. V152 is a prospective successor motivated solely by the already-visible provider fact that V151B used `temperature=0.0`. No V152 model output existed when this protocol was frozen.