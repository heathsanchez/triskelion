name: Metalogic Wake Sleep Staircase V3
on:
  push:
    paths:
      - 'experiments/METALOGIC_WAKE_SLEEP_STAIRCASE_V3.py'
      - '.github/workflows/metalogic-wake-sleep-v3.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  wake-sleep-v3:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      RIVER_API_KEY: ${{ secrets.RIVER_API_KEY }}
      PYTHONUNBUFFERED: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install frozen dependencies
        run: python -m pip install 'river-client==0.6.1' 'protobuf>=6.31.1,<7' transformers
      - name: Require River secret
        run: test -n "$RIVER_API_KEY"
      - name: Run confirmatory wake-sleep V3
        run: python experiments/METALOGIC_WAKE_SLEEP_STAIRCASE_V3.py
      - name: Upload scientific artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: metalogic-wake-sleep-staircase-v3
          path: artifacts/
          if-no-files-found: warn
