# Triskelion

Disposable private experimental harness for Metalogic verified developmental-learning tests.

Experiments are precommitted in source and run through GitHub Actions. Secrets are supplied only through GitHub Actions secrets and are never committed.
