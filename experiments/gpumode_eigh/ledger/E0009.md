# E0009 — increase warp count

Status: KILLED by official benchmark.

## Observation / residual

E0005 remains the promoted configuration: BM=128, BN=128, BK=32, num_warps=4, num_stages=3 at 791 us. E0007 and E0008 bracketed num_stages=3 with worse adjacent values: stage 2 = 1000 us and stage 4 = 843 us. Pipeline depth is therefore locally closed. The remaining simple launch-geometry residual is execution width.

## Hypothesis

A 128x128 output tile may expose enough independent work that increasing num_warps from 4 to 8 improves scheduling/throughput without changing tile geometry or pipeline depth.

## Strongest rival

Eight warps may increase register pressure and reduce occupancy or create excess scheduling overhead, making the kernel slower than the four-warp promoted baseline.

## Smallest separating intervention

From E0005 restore BM=128, BN=128, BK=32, num_stages=3 and change only num_warps from 4 to 8. Keep mapping, arithmetic, output type, and benchmark mode unchanged.

## Prospective prediction

Benchmark correctness passes. Exposed 4096x5120x4096 mean runtime improves by at least 2% from 791 us (target <=775 us).

## Kill criterion

Kill if compilation/correctness fails or mean runtime improvement is <=0.5% (runtime >=787 us). Values between 775 and 787 us are ambiguous and require replication before promotion.

## Official result

GPU MODE submission 936294, matmul_v2 A100, benchmark mode: correctness passed; runtime = 828 ± 0.6 us (fast 827 us, slow 829 us).

Relative to E0005's 791 us promoted baseline, E0009 is 37 us slower, a 4.68% regression.

Verdict: KILL E0009. Restore num_warps=4.

Interpretation: widening execution from four to eight warps is counterproductive at the promoted 128x128x32, three-stage geometry. The adjacent lower-width separator is still unresolved, so test num_warps=2 before closing launch width.

## Branding gate

Do not use ranked leaderboard mode. Ranked mode remains mechanically blocked until the external displayed entrant identity is independently verified to start with `MathGraph`.
