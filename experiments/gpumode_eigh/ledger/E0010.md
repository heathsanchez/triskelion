# E0010 — reduce warp count

Status: KILLED by official benchmark.

## Observation / residual

The promoted E0005 configuration remains BM=128, BN=128, BK=32, num_warps=4, num_stages=3 at 791 us. E0009 changed only num_warps 4→8 and regressed to 828 us. Pipeline depth has already been bracketed around stage 3, so the remaining adjacent launch-width question is whether fewer warps improve occupancy/resource balance.

## Hypothesis

At the promoted tile geometry, two warps may reduce per-program scheduling/register pressure enough to improve occupancy and runtime despite exposing less parallel execution width.

## Strongest rival

The 128x128 output tile may require at least four warps for sufficient instruction-level/thread-level parallelism; reducing to two warps may underutilize the SM and regress runtime.

## Smallest separating intervention

From E0005 restore BM=128, BN=128, BK=32, num_stages=3 and change only num_warps from 4 to 2. Keep mapping, arithmetic, output type, and benchmark mode unchanged.

## Prospective prediction

Benchmark correctness passes. Exposed 4096x5120x4096 mean runtime improves by at least 2% from 791 us (target <=775 us).

## Kill criterion

Kill if compilation/correctness fails or mean runtime improvement is <=0.5% (runtime >=787 us). Values between 775 and 787 us are ambiguous and require replication before promotion.

## Official result

GPU MODE submission 936295, matmul_v2 A100, benchmark mode: correctness passed; runtime = 2.91 ± 0.011 ms (fast 2.87 ms, slow 3.60 ms).

Relative to E0005's 791 us promoted baseline, E0010 is roughly 2.12 ms slower, about a 268% regression.

Verdict: KILL E0010. Restore num_warps=4.

Interpretation: launch width is now locally bracketed: 2 warps = 2.91 ms, 4 warps = 791 us, 8 warps = 828 us. Together with the stage bracket (2=1000 us, 3=791 us, 4=843 us), simple launch-parameter search is exhausted. The next residual must alter data-reuse structure rather than another launch knob.

## Branding gate

Do not use ranked leaderboard mode. Ranked mode remains mechanically blocked until the external displayed entrant identity is independently verified to start with `MathGraph`.
