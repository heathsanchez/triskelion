# Triskelion Progress Ledger — V49 through V56

_Date: 2026-08-14 NZST_

> **Status authority:** [`ATTESTATION_LEDGER.md`](ATTESTATION_LEDGER.md). This file preserves chronology. Earlier versions overstated V49–V51 before primary artifact/predicate audit; that audit is now complete.

## `SEALED_TRANSFER_V49` — ATTESTED WITH SEALING QUALIFICATION

Audited primary lineage:

- branch `v49-sealed-run`;
- run `31749682731`;
- head `79799fba8961645bcd20ba6f94bcf34011a8b629`;
- artifact `metalogic-v49-sealed`;
- digest `sha256:1079cae6f06085dc59bfa23cf548807d4219ecae811dd2d424dab5b9d3113a47`.

Executable behavior: unique learned lexical category; Requests cold FAIL → warm PASS → ablation FAIL; later in-category Django counterexample → `REVOKE`.

Qualification: temporal sealing is supported by phase-separated code/workflow order, not by the hard-coded `requests_absent_from_phase_a_code=True` flag.

## `OUTCOME_LABELED_V50` — ATTESTED WITH SEALING QUALIFICATION

Original run `31749833138`, head `929461fe989592f40f2e15cb0105d81785281817`, artifact digest `sha256:6a97a3e5978d03c5f495f01ca11c0620c822b12e13c963f7df31e01a7cdebddd`.

The calibration harness does **not** supply HELP/HARM class labels. It computes:

`HELP = (before FAIL, after PASS)`

`HARM = (before PASS, after FAIL)`.

Two HELP + two HARM episodes induce one lexical relation. Requests then gives cold FAIL → warm PASS → ablation FAIL and a later same-relation Django HARM triggers `REVOKE`.

Qualification: unseen/sealed flags are declarative; the temporal boundary is supported structurally by phase-separated workflow/code.

## `OPERATOR_INVENTION_V51` — ATTESTED WITH SEALING QUALIFICATION

Branch `v51-operator-invention`, run `31759857216`, head `f739be68af3cd6695a1674225938961f3b69f7b2`, artifact digest `sha256:e3c10a4ba1011876e5df7248de4f3cb468e90195f6c331e625c3bb5d6c2f93d0`.

The frozen old algebra contains token-position permutations, which preserve token-value multisets. Two executable obstruction episodes have repair survivor sets whose unique intersection is `<=`; the resulting `< -> <=` rewrite therefore lies outside that frozen old closure. The committed operator then gives Requests cold FAIL → warm PASS → ablation FAIL; later Django harm yields no surviving refinement and triggers `REVOKE`.

Claim boundary: constructed inside a supplied generic token-emission meta-language; do not claim unrestricted invention outside all meta-languages.

### Naming collision

The files `V49_EXTERNAL_QUOTIENT_REFINEMENT_RESULT.*`, `V50_EXTERNAL_QUOTIENT_REFINEMENT_RESULT.txt`, and `V51_RESULT.txt` belong to a separate quotient-refinement sequence.

## V52 / V52e — historical BugsInPy attempts — INCOMPLETE

Historical environment reconstruction, obsolete dependencies and localization/coverage eliminated too many worlds. No robust different-project operator reuse was established.

Verdict: `INCOMPLETE_V52E_HISTORICAL_COVERAGE_RATCHET`.

## V53 — historical clean-room ratchet — INCOMPLETE

Fixed/human source remained withheld during search, but too few usable worlds survived and no unique repair operator emerged under the frozen constructor.

## `TWO_GENERATION_COMPOUNDING_V54` — BOUNDED PASS

Branch `v54-compounding-ratchet`; run `31761530951`.

- before O1: zero cold one-rewrite survivors;
- after O1: O2 (`and -> or`) became discoverable;
- `A0`: FAIL;
- `A0 + O1`: FAIL;
- `A0 + O1 + O2`: PASS;
- `A0 + O2` with O1 ablated: FAIL.

Supported bounded statement: `O2 ∉ Discoverable(A0)` but `O2 ∈ Discoverable(A0 + O1)`.

## `HISTORICAL_COMPOUNDING_V55A` — INCOMPLETE

Only two usable historical worlds survived, both Black. Neither yielded a unique O1. Natural V54-shaped compounding was not reached.

## `CONSTRUCTOR_COMPOUNDING_V55B` — NEGATIVE under tested substrate

K0 could only rewrite existing tokens and could not repair a deletion-style obstruction. The frozen generic insertion-family meta-substrate failed to synthesize K1.

## `MODEL_CARD_V55C` — MIXED / NEGATIVE

Frozen 12-case Qwen2.5-Coder-0.5B applicability set:

- no memory: 6/12;
- raw memory: 5/12;
- verified capability card in prompt: 5/12.

The model over-applied the capability and failed protected negatives. Natural-language scope text is not sufficient execution control.

## `HISTORICAL_BLIND_EDIT_V56A` — HARNESS FAILURE

The broader rewrite/delete/insert clean-room search crashed on stale/out-of-range edit coordinates (`IndexError`). No scientific verdict.

## `CONSTRUCTOR_RETRY_V56B` — HARNESS FAILURE

Malformed intermediate insertion raised `IndentationError` and crashed the harness. No scientific verdict; V55B remains the constructor-level scientific negative.

## `VERIFIER_MODEL_LAYER_V56C` — BOUNDED PASS

Same small Qwen family and 12-case setup:

- no memory: 6/12;
- raw memory: 6/12;
- structurally verifier-controlled capability layer: 12/12.

Supported bounded architecture:

`model proposes -> explicit capability/scope layer -> external verifier -> admit/block`.

The overall V56 suite CI failed because V56A/B crashed; V56C’s status is the subexperiment result, not the suite conclusion.

## Chronology-level synthesis

Within V49–V56, the evidence progression is now mechanically cleaner than the earlier prose suggested:

`sealed category transfer -> verifier-derived outcome labels -> operator outside frozen old closure -> two-generation discoverability compounding -> natural-world coverage obstruction -> constructor-substrate negative -> explicit verifier-controlled model augmentation`.

The V49–V51 line is no longer evidence debt. Its remaining qualification is specifically about how temporal sealing is certified, not whether the executable behavioral chains occurred.

For exact allowed wording and all older/newer evidence families, use `ATTESTATION_LEDGER.md`.
