# LOGOS Alphabet / Grammar State — V78–V80

_Last updated: 2026-08-14 NZST_

This file is the compact evidence boundary for the current alphabet-collider programme. It supplements `ATTESTATION_LEDGER.md`; it does not upgrade the global full-stack claim by itself.

## Current structural hypothesis

The evidence no longer supports treating all ten historical labels as ten peer atoms. The better current decomposition is:

1. **semantic / epistemic state transforms** — candidate primitives such as `PROBE`, `RELATE`, `DISTINGUISH`, `GENERATE`, `TRANSDUCE`, `CONSTRAIN`, `SELECT`, `COMPOSE`, `RETAIN`;
2. **typed composition laws** — causal order constraints such as `CONSTRAIN -> SELECT`, `TRANSDUCE -> CONSTRAIN`, and `COMPOSE -> RETAIN`;
3. **higher-order control combinators** — `RECURSE` is currently better modeled as generic fixed-point/reapplication control than as a peer semantic atom;
4. **macros / organs** — verified recurrent programs that earn unit status only through matched-budget causal benefit, transfer and ablation.

This is a working ontology, not a universal cognitive basis.

## V78 / V78B — authored calibration

Branch: `v78-alphabet-collider`

Run: `31776731483`

Head: `7e82064908bf02e4ef0a0f4a161ae5240cac34cc`

Artifact: `9210172164`

Artifact SHA-256: `f4a4f2a7a913413d0d6e0e0b2c4f0fcdbfa8106bb343979d0cb647c1ba00f5df`

Boundary: **BOUNDED CALIBRATION ONLY.** The worlds were authored to exercise the proposed instruction set.

Key results:

- full historical ten solve 320/320 calibration worlds;
- minimum full-coverage subset is 10/10 under this authored grammar;
- order shuffle preserves only 450/3200 executions;
- semantic permutation mean success is 0.1875%;
- removing historical `PROBE` exposes 80/80 hidden-information residuals across two families;
- among five generic meta-transform candidates, only `reveal_one` closes all 80; candidate deletion closes 15;
- frequency is not law: forbidding `CONSTRAIN -> SELECT` loses 100/160 solves, while frequent `GENERATE -> DISTINGUISH`, `GENERATE -> COMPOSE`, and `SELECT -> RECURSE` lose zero;
- reifying causal two-step macros reduces primitive execution cost 540 -> 340; random/non-observed macro null mean saving is 0.667.

## V78C / V78D — primitive-vs-grammar falsification

Primary vendored result: `results/attested/V78D_RELATE_RECURSE_HARDENED_RESULT.json`.

Hidden-primitive recovery first exposed a real ambiguity: `RECURSE` tied a generic `INC_DEPTH` transformation. The stronger V78D protocol then replaced the shallow recursion proxy with unknown-depth iteration and hardened `RELATE` against positional/ID shortcuts.

### RELATE

- 400/400 for a generic pair-relation transform;
- best shortcut/null alternatives score 30–70/400;
- breaking the within-item pairing drops the relation method to mean 58.55/400 (max 76);
- the same generic relation constructor transfers 200/200 to a different relation family (sum rather than absolute gap).

Classification: **`SEMANTIC_RELATION_OPERATOR_SUPPORTED`** in this bounded calibration.

### RECURSE

- monolithic recursive implementation: 400/400;
- generic fixed-point/reapplication combinator over an existing one-step transform: 400/400;
- extensional equality on the tested iteration family: true;
- fixed-point controller transfers 13/13 across numeric, string and list processes;
- fixed finite unrolling degrades with required depth (8 -> 28/400, 16 -> 92/400, 32 -> 196/400).

Classification: **`CONTROL_GRAMMAR_COMBINATOR_SUPPORTED`** in this bounded calibration.

Allowed wording: the current tests support moving `RECURSE` from a provisional peer semantic atom to a grammar/control-combinator hypothesis. They do not prove a universal ontology of recursion.

## V79 — source-distinct external PROBE reconstruction

Branch: `v79-external-probe`

Run: `31776900627`

Head: `622565dbbeada64bb510fc1ea4556ab63c8e4ac8`

Artifact: `9210205160`

Artifact SHA-256: `a72bd939c2009abe87bfb3d48117ae54f23be1b009ac17cd29f3ba4cc20ed17e`

Primary vendored result: `results/attested/V79_EXTERNAL_BLACKBOX_PROBE_RESULT.json`.

External callable families: CPython `math`, NumPy, SciPy special, and held-out mpmath. Candidate identities are opaque to the solver. Before a target-conditioned world observation, public state is identical across hidden targets, so the frozen passive closure cannot identify every target.

Results:

- passive all-target guarantee: 0 for every family under the indistinguishability obstruction;
- hand information-producing query: all targets identified, one call each;
- generic meta-candidate training on CPython + NumPy + SciPy: `SAMPLE_INTERFACE` 16/16, nearest deletion controls 2/16;
- unique selected generic operation: `SAMPLE_INTERFACE`;
- held-out mpmath: 6/6;
- ablation on mpmath: 0/6;
- semantic mapping shuffle: 0/6;
- verdict: `PASS_EXTERNAL_PROBE_RECONSTRUCTION_V79`.

**Qualification:** this is not unrestricted autonomous invention of an epistemic primitive. The meta-substrate supplied a finite generic candidate set that included an interface-sampling operation. The result shows blind, source-distinct selection/reuse of the needed information-producing operation under a real external callable interface.

## V80 — developmental consequence of PROBE-like O1

Branch: `v80-probe-compounding`

Run: `31777077312`

Head: `1f70278de25b0be35eea639e044d7626cd36bd2d`

Artifact: `9210272732`

Artifact SHA-256: `ab5371e7d112bc6ba598428468181f5af7afaf5ed683156dfad4499d79fe4788`

Primary vendored result: `results/attested/V80_PROBE_TO_SURROGATE_COMPOUNDING_RESULT.json`.

Using held-out mpmath functions and an unchanged generic Chebyshev constructor `K0`:

- without O1 / interface sampling: no target-conditioned observations, no O2 surrogate constructible, 0/6;
- with O1: 17 verified samples permit O2 construction, 6/6 pass on 41 fresh test points;
- O1 ablation: O2 no longer constructible, 0/6;
- serialized O2 rebuilt from capability payload alone: 6/6 with zero future sample-interface calls;
- wrong-lineage surrogate: 0/6;
- verdict: `PASS_EXTERNAL_O1_TO_O2_V80`.

**Qualification:** O2 is a constructed standalone capability under a supplied generic approximation constructor; it is not a newly invented primitive and this is not yet the natural heterogeneous V54 crown-jewel lineage.

## What is now supported

The combined V78–V80 programme supports, in bounded form:

`semantic transforms + causal grammar laws + higher-order control combinators + promotable macros`

rather than a flat ten-word ontology.

It also provides an external bridge:

`missing target-conditioned observation -> select PROBE-like operation -> source-distinct transfer -> later standalone capability becomes constructible -> ancestor ablation removes that constructibility`.

## What remains open

1. **Autonomous primitive synthesis:** construct `omega*` from a generic transformation language rather than selecting it from a finite menu containing interface sampling.
2. **Natural heterogeneous metalanguage pressure:** independently authored tasks should force the same missing epistemic transformation without a black-box-identification task being designed around it.
3. **Natural O1 -> O2 -> O3 lineage:** later capability discovery should compound across pre-existing heterogeneous tasks with ancestor ablations.
4. **External audit of the rest of the proposed semantic alphabet:** determine which labels are genuinely irreducible semantic transforms and which migrate into grammar, type, lifecycle or organ layers.
5. **Verified organ formation:** promote recurrent causal programs only when unit invocation produces matched-budget benefit, transfer and ablation.

The current target is therefore not `prove the ten words universal`. It is to recover the smallest evolving typed instruction set and its lawful composition grammar from causal external evidence.
