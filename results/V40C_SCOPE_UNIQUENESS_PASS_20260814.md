# V40c — PASS

Run `31739497866`.

V40b hash-ranked Rich production `<` sites outside `If.test`/`IfExp`. The first eligible site, `rich/highlighter.py: cursor < len(plain)`, failed Rich's unchanged full suite after `< -> <=`: 2 failed, 954 passed, 25 skipped.

Combining that protected evidence with the prior repository-test-backed trigger, Requests transfer, Click protection, and later Django counterexample leaves exactly one minimal predicate among 18 frozen generic candidates: `IN_IF_TEST=1`.

After later counterevidence, zero predicates remain consistent and the decision is `REVOKE`.

Verdict: `PASS_V40C_UNIQUE_SCOPE_IDENTIFICATION`.

Boundary: `IN_IF_TEST` remains a supplied structural feature, so this is unique construction within the feature substrate, not feature invention outside it.
