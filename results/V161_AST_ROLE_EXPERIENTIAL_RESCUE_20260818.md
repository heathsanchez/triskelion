# V161 AST-role Experiential Rescue — 2026-08-18

## Status
PASS — bounded AST-role experiential rescue across heterogeneous identifier names.

Workflow run: `32060890023`
Head SHA: `5011d53966618c37ec8d28cc43c13db5e2371e7f`
Artifact: `v161-ast-role-experiential-rescue`
Artifact SHA256: `8a7d48afbda7262805584e69c0865a035193e9087392f908217a1e0b68d82c4b`

## Prior executable-verified experience
Stable pre-interference AB checkpoint produced and executable verification admitted:

A:
- `operators.append(symbol)` on source roles `{stack=operators, loop_item=symbol}`
- `holding.append(entry)` on source roles `{stack=holding, loop_item=entry}`
- both induce `A_APPEND_STACK_LOOP_ITEM`

B:
- `return [candidate]`
- `return [value]`
- both induce `B_RETURN_SINGLETON_ARG`

The installed map is therefore over source/AST roles rather than world-id substitution.

## Protected frozen-weight evaluation
Eight new heterogeneous-name programs per task were evaluated at the V160 first-regression checkpoint.

A+B executable verifier hits out of 16:
- neural only: **8/16**
- raw verified memory: **8/16**
- compiled experiential AST-role map: **16/16**
- shuffled map: **0/16**

Per task:
- neural: A 1/8, B 7/8
- raw memory: A 0/8, B 8/8
- compiled map: A 8/8, B 8/8
- shuffled map: A 0/8, B 0/8

The dominant neural residual remains malformed termination/delimiter completion. For A, most outputs contain the correct semantic repair body but omit the final `)`; B is more robust under heterogeneous names, with only one such failure in neural-only and raw memory repairing that one case.

## Verdict
`PASS_AST_ROLE_EXPERIENTIAL_RESCUE`

## Supported bounded statement
Under this frozen executable QuixBugs-derived repair protocol, prior verifier-admitted coding experiences can be compiled into AST/source-role operators that transfer across unrelated identifier names and restore protected executable behavior at a later regressed neural checkpoint. The compiled role map achieves 16/16 versus 8/16 for frozen neural weights, 8/16 for raw verified memory, and 0/16 for a same-budget shuffled-map ablation.

## Claim boundary
The role vocabulary is supplied and small. This is not unrestricted AST abstraction invention or unrestricted program repair. The result removes V160's numeric alpha-renaming scaffold and establishes that the causal object can be a structural role-level repair map rather than a concrete answer cache.

## Next separator
Do not spend more experiments on identifier renaming. The next valuable question is whether prior verified structure improves **discovery of a new repair/operator that the stored map does not itself encode**, rather than merely executing a retained repair schema. This should combine the V54 discoverability criterion with V161's executable model/AST setting: cold map cannot discover O2 under a fixed proposal budget; after verified O1 is installed, O2 becomes discoverable; ablate O1 and the gain disappears.
